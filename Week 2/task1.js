// Declare a variable age and assign it a value of 25. The, declare another variable isAdult that is true if age is 18 or more otherwise false.


let age = 25;
let isAdult;

if (age >= 18) {
    isAdult = true;
} else {
    isAdult = false;
}
console.log(isAdult); 
